symstore.txt
设置符号服务器


clearjit.bat
清除JIT设置


DisableAuto.bat
清除JIT设置


edit_local_dumps_key.cmd
edit_local_dumps_key.cmd -df c:\dumps –dt 2
The first argument to this helper script is the folder location that WER should use when it
generates its user-mode crash dumps, and the second argument specifies the type of those dump
files. A value of 2 indicates that you would like WER to generate crash dumps with full memory information


